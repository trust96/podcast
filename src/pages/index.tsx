import React from 'react';
import Button from './components/Button';
import Logo from './components/logo';

interface Props {}

const index = (props: Props) => {
	return (
		<>
			<Logo className="" /> <Button></Button>
		</>
	);
};

export default index;
